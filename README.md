# namaste_react

practicing react concept

Two types of Export/Import

Default Export/Import: When we have only one thing to export from a file

export default Component;
import Component from "path";

Named Export/Import: when we have multiple things to export from a file

export const Component;
import {Component} from "path";
